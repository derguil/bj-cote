#include <iostream>
using namespace std;

int main() {
	int a,b,c,d;
	cin >> a >> b >> c;
	set<int> e = {a,b,c};
	array<int> f = [a,b,c]
	if(e.size() == 1){
		d = 10000+a*1000;
	}
	if(e.size() == 2){
		foreach( (e)=>g ) {
		f.remove(g)
		}
		d = 1000 + f[0]*100
	}
	if(e.size() == 3){
		d = max(a,b,c)*100
	}

	cout << d;

	return 0;
}